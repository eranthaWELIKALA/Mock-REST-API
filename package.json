{
  "name": "request-handler",
  "version": "1.0.1",
  "description": "A server that can be used to test and handle a response of a request",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Erantha Welikala",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.17.1",
    "mongodb": "^6.8.0",
    "mongoose": "^8.5.1"
  }
}
